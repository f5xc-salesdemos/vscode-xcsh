metadata:
  name: example-app-lb
  namespace: app-ns
spec:
  domains:
    - my-app.example.com
  http:
    dns_volterra_managed: true
  default_route_pools:
    - pool:
        name: example-app-pool
        namespace: app-ns
      weight: 1
